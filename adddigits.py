num = 0
snum = str(num)
lsum = []
for n in snum:
    lsum.append(int(n))
print("hello")

while len(lsum) > 1:
    num = 0
    for l in lsum:
        num += l
    snum = str(num)
    lsum = []
    for n in snum:
        lsum.append(int(n))
print(lsum[0])
# break

#     res = []
#     for n in nn:
#         res
